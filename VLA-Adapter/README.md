# AsyncMixVLA Adapter runtime

This directory contains the inference-only VLA-Adapter service used by AsyncMixVLA. Use the repository-level [`README.md`](../README.md) for installation and launch commands.

The upstream training and standalone experiment programs have been intentionally omitted. The preserved `prismatic` files are the model components required to load and serve the Adapter checkpoint.
